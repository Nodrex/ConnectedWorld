<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/actionBar</item> <!-- Action bar color-->
        <item name="colorPrimaryDark">@color/statusBar</item> <!-- Status bar color -->
        <item name="colorAccent">@color/fab</item> <!-- FloatingActionButton color -->
        <!--<item name="colorControlActivated">@color/green</item>-->
        <item name="colorControlActivated">@color/fab</item>

        <!--<item name="android:windowBackground">@drawable/splash_screen</item>-->

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
